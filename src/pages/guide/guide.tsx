import 'styled-components'

import { ContentLogo } from '../../components'
import { Section } from '../../styles/globalStyles'

function GuidePage() {
  return (
    <Section display="flex" justify="space-between" align="center">
      <ContentLogo />
    </Section>
  )
}

export default GuidePage
